import Tester as test
# import tkinter as tk
# window = tk.Tk()

# window.title('Attendance system')
# window.geometry("700x800+500+50")
data=test.get_path()
test.recognizer(data)
# lb= tk.Listbox(window, height=5,selectmode='single')
# for i in data:
#     lb.insert(tk.END,i)
# lb.grid(row=10,column=10)
# window.mainloop()
